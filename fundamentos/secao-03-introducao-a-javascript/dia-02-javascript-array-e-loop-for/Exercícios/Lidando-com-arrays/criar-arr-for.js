let arr = [];
let div = 0;

//criando array através do for.

for (let index = 0; index <= 25; index += 1) {
  arr.push(index);

  //dividindo por 2 os elementos do array criado.
  console.log(arr[index] / 2);
}
